import * as React from 'react';
import { BlogLoadingContainer } from './LoadingStyled';

interface IBlogLoadingProps {
}

const BlogLoading: React.FunctionComponent<IBlogLoadingProps> = (props) => {
  return (
	  <BlogLoadingContainer>
		  
	  </BlogLoadingContainer>
  );
};

export default BlogLoading;
